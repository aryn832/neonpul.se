version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18.14.1
        - nvm use 18.14.1
    build:
      commands:
        - npm run build
artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

